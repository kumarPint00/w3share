version: '3.8'

services:
  contract-admin-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
